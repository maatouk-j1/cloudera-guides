# CLAUDE.md

## Agent skills

### Issue tracker

Issues are tracked as GitHub Issues on `maatouk-j1/cloudera-guides` via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Default canonical labels (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`). See `docs/agents/triage-labels.md`.

### CDP 7.3.2 ingestion

Ingesting the Cloudera 7.3.2 ECS deployment guide? Follow `docs/agents/cdp-7-3-2-ingestion.md` — the authoritative phase index, PDF toolchain rules, and MDX conventions.

### Domain docs

Single-context: `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
