---
title: Solution Summary
topicTitle: CDP 7.3.2
topicSlug: cdp-7-3-2
summary: Introduction, cluster architecture, service dependencies, prerequisites, and the hardware and software requirements for a Cloudera on premises 7.3.2 deployment with ECS.
prevTitle: VM Creation
prevSlug: /installations/cdp-7-3-2/vm
nextTitle: Post OS Installation - Preliminary Work
nextSlug: /installations/cdp-7-3-2/post-os-work
---

This guide provides all the required information to set up and install Cloudera on premises. This page introduces the platform, the shape of the deployment, and the prerequisites, hardware and software you need before any installation work begins.

---

## Important URLs

**Install Cloudera on premises Base and Data Service Clusters:**

- [Cloudera on premises Base installation](https://docs.cloudera.com/cdp-private-cloud-base/7.3.2/installation/topics/cdpdc-installation.html)
- [Data Services installation on ECS](https://docs.cloudera.com/cdp-private-cloud-data-services/latest/installation-ecs/topics/cdppvc-installation-ecs-steps.html)

**Uninstall and cleanup Cloudera on premises Base, Data Service Clusters and PostgreSQL DB:**

- [Cloudera on premises Base uninstallation](https://docs.cloudera.com/cdp-private-cloud-base/7.3.2/installation/topics/cdpdc-uninstallation.html)
- [Uninstall Data Services on ECS](https://docs.cloudera.com/cdp-private-cloud-data-services/1.5.5/installation-ecs/topics/cdppvc-installation-ecs-uninstall-pvc.html)
- [How to completely uninstall PostgreSQL](https://kb.objectrocket.com/postgresql/how-to-completely-uninstall-postgresql-757)

## Introduction

Cloudera on premises is an integrated analytics and data management platform deployed in private data centers. Cloudera Data Platform is a single platform that has two form factors: CDP Public and Cloudera on premises. It consists of Cloudera on premises Base and Cloudera on premises Data Services, and offers broad data analytics and artificial intelligence functionality along with secure user access and data governance features.

Cloudera on premises (PvC) data services components run on containerized clusters and thus require a container orchestration engine to manage all the workloads.

There will be two major components in a Cloudera on premises installation:

- Cloudera on premises Base Cluster
- Cloudera on premises Data Services Cluster

Cloudera on premises DS offers installation with two orchestration engines:

- RedHat Openshift Container Platform (OCP)
- **Embedded Container Service (Cloudera managed-ECS)**

This guide focuses on **Cloudera on premises Data Service Cluster** setup with **ECS**.

## Cluster Architecture

A Cloudera on premises setup consists of the following three parts.

![Architecture diagram showing a FreeIPA server as the identity and authentication provider above a CDP Base Cluster of pvcmaster and pvcbase1-3 nodes and a CDP ECS Cluster of ecsmaster and ecsworker1-5 nodes](/images/cdp-7-3-2/summary/architecture-overview.png)

### FreeIPA / AD server

Provides the identity and authentication to the cluster. It includes Kerberos as the authentication provider and LDAP as the directory service provider. All the cluster nodes (both Base and ECS) act as FreeIPA agents. The FreeIPA server includes Private DNS Server, MIT Kerberos KDC, Directory Server, Chrony, Dogtag certificate system and SSSD.

### CDP Base Cluster

Consists of all the prerequisite services that form the basis for the CDP Data Lake for Data Services:

- Atlas
- Solr
- HBase
- HDFS
- Hive (Metastore Server)
- Hive-on-Tez (HiveServer2)
- Hue (Required for CDW data service)
- Iceberg Replication
- Impala (Used as Client)
- Kafka
- Ozone (Required for CDE data service)
- Phoenix
- Ranger
- Spark on YARN (Spark 2)
- Spark 3
- Tez
- YARN
- Yarn Queue Manager (Optional)
- ZooKeeper

Missing components which are not considered as part of this setup guide, but can be installed additionally if needed:

- Flink
- Knox
- Kudu
- Livy
- Nifi
- Nifi Registry
- QueueManager
- S3 Connecter
- Zeppelin

### CDP ECS Data Services Cluster

This is the Rancher (RKE) based Kubernetes cluster that forms the basis for all the containerized workloads of CDP Data Services. It consists of ECS master, ECS agents, and Docker servers.

### Service Dependencies

| Service | Dependencies |
|:--|:--|
| Atlas | HDFS, HBase, Kafka (Kafka broker role only), Solr |
| DataViz | - |
| HBase | HDFS, ZooKeeper |
| HDFS | Hive, Spark, Yarn |
| Hive | HDFS, YARN, Tez |
| Hive-on-Tez | HDFS, YARN, Hive, Tez |
| Hue | HDFS, Hive |
| Impala | HDFS, Hive |
| Kafka | ZooKeeper |
| Livy | Spark, Impala |
| Nifi | Knox, Ranger |
| Nifi Registry | Nifi, Knox, Ranger |
| Oozie | YARN |
| Ozone | - |
| Ranger | HDFS, Solr, Atlas |
| Solr | HDFS, ZooKeeper |
| Spark on YARN | YARN |
| Streams Messaging Manager | Kafka |
| Streams Replication Manager | Kafka |
| Tez | YARN |
| YARN | HDFS, ZooKeeper |
| Zeppelin | HDFS, Spark-on-YARN, YARN |
| ZooKeeper | - |

## Prerequisites

This reference architecture details the process of installing Cloudera on premises on bare metal or VM based servers, and the configuration details of fully tested and validated workloads in the cluster.

### Entitlements

Your license key must have the PvC DS entitlement. A current key without the entitlement will block access to ECS bits. Raise a ticket or reach out to the Cloudera POC to get the necessary entitlements.

### Virtual Machines

Administrator access to virtual machines.

### Infrastructure Setup: Hardware and Software Requirements

The table below summarizes the machines used for this POC. This is a minimum requirement: one can increase the number of machines to achieve High Availability and Fault Tolerance. If this cluster is not meant to perform any benchmarking or performance test, one can proceed ahead with this infrastructure.

<Banner type="important">
  The cluster configurations used in this guide are designed and decided considering the installation, configuration and management of all 3 data services (CAI, CDW, CDE) with minimalistic workloads on a single ECS data services cluster for PoC purposes. The hardware specs should be redetermined and recalculated for clusters set up for a different purpose.
</Banner>

Screenshots shown and versions used in this guide are for reference purposes only and may differ from the versions you use.

| Count | CDP Role |
|:--|:--|
| 1 | FreeIPA or AD Server (Will be used for FreeIPA, Kerberos, Private DNS, LDAP, NTP, KDC, and will be used as an ansible controller node for automation purpose) |
| 1 | Cloudera-Manager Server (with external PostgreSQL Database server, will be used for downloading bits as well) |
| 4 | CDP Base Cluster (1 Master and 3 Worker Nodes) |
| 3-11 | CDP ECS Data Services Cluster (1 Master and 3-10 Worker Nodes) |

### DNS Server (In case we are not going with FreeIPA)

An external DNS server must contain the forward and reverse zones of the company domain name. The external DNS server must be able to resolve the hostname of all Cloudera on premises hosts and the 3rd party components (includes Kerberos, LDAP server, external database, NFS server) and perform reverse DNS lookup.

A **wildcard DNS** entry must be configured; for example `*.apps.cldrsetup.local`. This helps to reduce Day-2 operational tasks to set separate DNS entries for each newly provisioned external-facing application/service.

The external DNS server is expected to be ready prior to installing the Cloudera on premises solution, and its installation procedure is not covered in this guide.

![Diagram of a wildcard DNS entry resolving the apps subdomain through the ingress to two ECS service and pod groups on separate node subnets](/images/cdp-7-3-2/summary/wildcard-dns.png)

### Kerberos + LDAP Server/AD + Certificate (Required only, in case we are not going with FreeIPA)

An external Kerberos server and the Kerberos key distribution center (KDC), with a realm established, must be available to provide authentication to CDP services, users and hosts.

An external secured LDAP-compliant identity/directory server (LDAPS) is required to enable the Cloudera on premises solution to look up user accounts and groups in the directory. This is expected to be ready prior to installing the Cloudera on premises solution, and its installation procedure is not covered in this guide.

Auto-TLS should be enabled using certificates created and managed by a Cloudera Manager certificate authority (CA), or certificates signed by a trusted public CA or your own internal CA. Prepare the certificate of your choice.

The total number of CA certificates must not exceed 10. Otherwise, pods will be evicted during initialization due to limited memory (1Gi) to process the configmap file.

For the Nutanix setup, this guide uses the existing **Active Directory (AD) server** available within the Nutanix cluster for configuring Cloudera Base and Cloudera Data Services.

### External NFS (Preferable but optional; needed for CAI use case)

CAI requires an external NFS server to store the project files and directories. NFS version 4.1 must be supported.

The external NFS storage is expected to be ready prior to installing the Cloudera on premises solution. External NFS storage installation is not covered in this guide.

![Diagram of vSphere/vSAN cluster 1 for CDP Base, hosting the PVC MASTER and PVC BASE1-3 servers across four hosts](/images/cdp-7-3-2/summary/vsphere-cluster-base.png)

![Diagram of vSphere/vSAN cluster 2 for CDP Data Experience, hosting the PVC ECS1-4 servers across four hosts](/images/cdp-7-3-2/summary/vsphere-cluster-data-services.png)

## Hardware Requirements

Hardware specs (CPU, memory, disk, and so on) should be analyzed and re-determined as per the setup requirement: POC, demo, HA, DR. The setup below is for POC/Demo purposes only. See the [Cloudera requirements and supported versions](https://docs.cloudera.com/cdp-private-cloud-base/7.3.2/installation/topics/cdpdc-requirements-supported-versions.html).

### FreeIPA/Kerberos & Private DNS Server

In case we are not going with FreeIPA, an external Kerberos/KDC is required.

| Role | HostName | CPU | RAM | Disk | Partitions (recommended but not mandatory) |
|:--|:--|:--|:--|:--|:--|
| ipaserver/AD +ansible-controller | `ipaserver` | 16 | 32GB | OS disk (250GB) | root partition |
| cldr-mngr, postgres db, bits | `cldr-mngr` | 32 | 64GB | 800GB | root partition, `/var`=600GB, `/opt`=600GB |
| Bastion server | `jump` | 8 | 16GB | 200GB | root partition |

<Banner type="important">
  Each of the nodes in the configurations below requires a dedicated minimum allocation of 450GB each for `/docker` and `/var`, important to consider if dedicated mounts (disks) are used.
</Banner>

### PvC Base Cluster: a 4-node cluster on VMs

The BaseMaster node also hosts the Gateway and Utility hosts' services as per the [public documentation on runtime cluster host role assignments](https://docs.cloudera.com/cdp-private-cloud-base/7.3.2/installation/topics/cdpdc-runtime-cluster-hosts-role-assignments.html). The role assignment strategy for Control Plane Services (HDFS, YARN, Spark, and so on) is discussed in the later steps of the PvC Base cluster setup.

| Role | HostName | CPU | RAM | Disk | Partitions |
|:--|:--|:--|:--|:--|:--|
| Base-Master | `pvcbase-master` | 32 | 64GB | root partition (600GB) | `/hdfs` `/opt` `/var` `/yarn` |
| Base-Worker | `pvcbase-worker1` | 32 | 64GB | root partition (600GB) | `/hdfs` `/opt` `/var` `/yarn` |
| Base-Worker | `pvcbase-worker2` | 32 | 64GB | root partition (600GB) | `/hdfs` `/opt` `/var` `/yarn` |
| Base-Worker | `pvcbase-worker3` | 32 | 64GB | root partition (600GB) | `/hdfs` `/opt` `/var` `/yarn` |

### PvC Data Service (ECS) Cluster with CDW: a 4-node cluster on VMs

Specs are upgraded for concurrent tests and higher data volume tests, and assume only CDW services will be deployed: 1 CDW Data Catalog, 1 CDV (DataViz) Small Instance, 1 Hive LLAP and 1 Impala Virtual Warehouse, each with 1 coordinator and 2 executors.

| Role | HostName | CPU | RAM | Disk | Partitions |
|:--|:--|:--|:--|:--|:--|
| ECS-Master | `pvcecs-master` | 32 | 128GB | root partition + 1.3 TB | `/cdwdata`, `/docker`, `/lhdata`, `/var` |
| ECS-Worker | `pvcecs-worker1` | 32 | 256GB | root partition + 1.3 TB | `/cdwdata`, `/docker`, `/lhdata`, `/var` |
| ECS-Worker | `pvcecs-worker2` | 32 | 256GB | root partition + 1.3 TB | `/cdwdata`, `/docker`, `/lhdata`, `/var` |
| ECS-Worker | `pvcecs-worker3` | 32 | 256GB | root partition + 1.3 TB | `/cdwdata`, `/docker`, `/lhdata`, `/var` |

### PvC Data Service (ECS) Cluster with CAI: a 3-node cluster on VMs

Specs are upgraded for concurrent tests and higher data volume tests, and assume only CAI services will be deployed: 1 CAI Workbench with 10 small and 2 average sized CAI concurrent sessions.

| Role | HostName | CPU | RAM | Disk | Partitions |
|:--|:--|:--|:--|:--|:--|
| ECS-Master | `pvcecs-master` | 32 | 128GB | root partition + 1.3 TB | `/docker`, `/lhdata`, `/var` |
| ECS-Worker | `pvcecs-worker1` | 32 | 128GB | root partition + 1.3 TB | `/docker`, `/lhdata`, `/var` |
| ECS-Worker | `pvcecs-worker2` | 32 | 128GB | root partition + 1.3 TB | `/docker`, `/lhdata`, `/var` |

### PvC Data Service (ECS) Cluster with CDE: a 6-node cluster on VMs

Specs are upgraded for concurrent tests and higher data volume tests, and assume only CDE services will be deployed: 1 CDE Virtual service along with 1 Virtual Cluster with 5 small and 2 average sized CDE concurrent jobs.

| Role | HostName | CPU | RAM | Disk | Partitions |
|:--|:--|:--|:--|:--|:--|
| ECS-Master | `pvcecs-master` | 32 | 128GB | root partition + 1.3 TB | `/docker`, `/lhdata`, `/var` |
| ECS-Worker | `pvcecs-worker1` | 32 | 128GB | root partition + 1.3 TB | `/docker`, `/lhdata`, `/var` |
| ECS-Worker | `pvcecs-worker2` | 32 | 128GB | root partition + 1.3 TB | `/docker`, `/lhdata`, `/var` |
| ECS-Worker | `pvcecs-worker3` | 32 | 128GB | root partition + 1.3 TB | `/docker`, `/lhdata`, `/var` |
| ECS-Worker | `pvcecs-worker4` | 32 | 128GB | root partition + 1.3 TB | `/docker`, `/lhdata`, `/var` |
| ECS-Worker | `pvcecs-worker5` | 32 | 128GB | root partition + 1.3 TB | `/docker`, `/lhdata`, `/var` |

### PvC Data Service (ECS) Cluster with CDW+CDE+CAI: an 11-node cluster on VMs

Specs are upgraded for concurrent tests and higher data volume tests, and assume all 3 services will be deployed (CDW, CDE, CAI), sized for 1 CDW Data Catalog, 1 CDV (DataViz Small) Instance, 1 Hive LLAP and 1 Impala Virtual Warehouse each with 1 coordinator and 2 executors; 1 CAI Workbench with 10 small and 2 average sized CAI concurrent sessions; and 1 CDE Virtual service along with 1 Virtual Cluster with 5 small and 2 average sized CDE concurrent jobs.

| Role | HostName | CPU | RAM | Disk | Partitions |
|:--|:--|:--|:--|:--|:--|
| ECS-Master | `pvcecs-master` | 32 | 128GB | root partition + 1.3 TB | `/cdwdata`, `/docker`, `/lhdata`, `/var` |
| ECS-Worker | `pvcecs-worker1` | 32 | 128GB | root partition + 1.3 TB | `/cdwdata`, `/docker`, `/lhdata`, `/var` |
| ECS-Worker | `pvcecs-worker2` | 32 | 128GB | root partition + 1.3 TB | `/cdwdata`, `/docker`, `/lhdata`, `/var` |
| ECS-Worker | `pvcecs-worker3` | 32 | 128GB | root partition + 1.3 TB | `/cdwdata`, `/docker`, `/lhdata`, `/var` |
| ECS-Worker | `pvcecs-worker4` | 32 | 128GB | root partition + 1.3 TB | `/cdwdata`, `/docker`, `/lhdata`, `/var` |
| ECS-Worker | `pvcecs-worker5` | 32 | 128GB | root partition + 1.3 TB | `/cdwdata`, `/docker`, `/lhdata`, `/var` |
| ECS-Worker | `pvcecs-worker6` | 32 | 128GB | root partition + 1.3 TB | `/cdwdata`, `/docker`, `/lhdata`, `/var` |
| ECS-Worker | `pvcecs-worker7` | 32 | 128GB | root partition + 1.3 TB | `/cdwdata`, `/docker`, `/lhdata`, `/var` |
| ECS-Worker | `pvcecs-worker8` | 32 | 128GB | root partition + 1.3 TB | `/cdwdata`, `/docker`, `/lhdata`, `/var` |
| ECS-Worker | `pvcecs-worker9` | 32 | 128GB | root partition + 1.3 TB | `/cdwdata`, `/docker`, `/lhdata`, `/var` |
| ECS-Worker | `pvcecs-worker10` | 32 | 128GB | root partition + 1.3 TB | `/cdwdata`, `/docker`, `/lhdata`, `/var` |

## Reference Architecture

This reference architecture explains the architecture and deployment procedures for Cloudera Data Platform on premises, using on-premises infrastructure for big data and analytics. It provides the details to configure Cloudera on premises on bare metal RHEL 9 based infrastructure.

The versions it is built and validated against are listed in the software requirements table below.

## Software Requirements

<Banner type="important">
  This guide is written for the versions specified below, and the commands to be executed are specific to those versions. If you plan to use different versions, commands may need to be updated separately.
</Banner>

The table below lists the software components and versions required for a single cluster of the servers running on premises, as tested and validated in this guide.

| Software Component | Version or Release | Host to be Installed |
|:--|:--|:--|
| OS: Red Hat Enterprise Linux Server (RHEL) | 9.5 (Verify with SupportMatrix first) | All Servers |
| OpenJDK | `17.0.14.0.7-1` >= | All Servers |
| Python3 | `3.9.22` >= | All Servers |
| PostgreSQL DB | 14 >= | Cldr-Mngr |
| Psycopg2-binary | `2.9.11` >= | All Servers |
| Postgres-JDBC-Connector | `42.7.7` >= | All Servers |
| Cloudera Manager | 7.13.2.0 (`7.13.2.0-77091850`) | Cldr-Mngr |
| Cloudera on premises Base (RunTime) | 7.3.2.0 (`7.3.2-1.cdh7.3.2.p0.77083870`). Install Base Runtime 7.3.2 **only if you are not planning to use CAI** (Data Service), otherwise install **7.3.1 SP3 CHF2** | PvC Base Cluster Nodes |
| Cloudera on premises Data Services | 1.5.5 SP2 (`1.5.5-h2000-b238`) | PvC Data Service Cluster Nodes |
| Hadoop (Includes YARN and HDFS) | `3.4.2.7.3.2.0-957` | PvC Base Cluster Nodes |
| Spark3 | `3.5.4.7.3.2.0-957` | PvC Base Cluster Nodes |
| Ozone | `2.0.0.7.3.2.0-957` | PvC Base Cluster Nodes |
| FreeIPA Server (Only if you are going with FreeIPA) | Latest: 4.13.0 | IPA server node |
| FreeIPA Client (Only if you are going with FreeIPA) | Latest: 4.13.0 | All nodes except ipaserver |
| NFS Utility Package | Latest: 2.8.3 | PvC Data Service Cluster Nodes |
| TLS | AutoTLS (Self-signed) | ipa server node |
| Kerberos + LDAP(IdP) + DNS | FreeIPA | ipa server node |
| Data Lake Storage | HDFS (All), Ozone, Iceberg v2 (with HDFS & Ozone) | |
| ECS DB Configuration | Embedded | ECS Only |
| Vault | Embedded | ECS Only |
| Docker Registry Type | Embedded/ Cloudera Default | ECS Only |
| NFS for CAI | Embedded i.e Internal | ECS Only |

Further references:

- [Requirements and supported versions, including product compatibility matrices](https://supportmatrix.cloudera.com/), and the [Cloudera on premises Base 7.3.2 documentation index](https://docs.cloudera.com/cdp-private-cloud-base/7.3.2/index.html)
- [Cloudera on premises Base and Experiences versions and supported features](https://docs.cloudera.com/cdp-private-cloud-base/7.3.2/private-release-notes/topics/rt-runtime-component-versions_732.html)
- [Cloudera on premises Base requirements and supported versions](https://docs.cloudera.com/cdp-private-cloud-base/7.3.2/installation/topics/cdpdc-requirements-supported-versions.html)

Dedicated **NVMe/SSD drives** are recommended to store Ozone metadata and Ozone mgmt configuration for the admin/mgmt nodes and worker/data nodes, and CDW data service storage for virtual warehouses for local attached Storage Tiering Cache.

## Node Names

The table below contains the names assigned to the VM instances and to some other required components. The rest of this guide refers to them by name.

The domain name and the hostnames mentioned here are for reference only. You may choose hostnames as per your requirements.

| NodeName | Details |
|:--|:--|
| `pvcbase-master` | Cloudera on premises Base Master |
| `pvcbase-worker1` to `pvcbase-worker4` | CDP Base Cluster Worker Nodes |
| `ipaserver` (OR Existing LDAP/AD + DNS + Kerberos + KDC) | FreeIPA / Existing AD Server |
| `cldr-mngr` | Cloudera-Manager and PostgreSQL DB Server |
| `pvcecs-master` | ECS Master Node |
| `pvcecs-worker1` to `pvcecs-worker7` | ECS Worker Nodes |
| `CLDRSETUP.LOCAL` (Replace with your ORG DOMAIN) | Dummy Domain For POC Purpose |

Once you are familiar with all the information above, you can start with the preliminary work for the CDP Base cluster setup.
